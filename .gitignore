# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/

# Training outputs (weights stay in R2 + NAS, not git)
models/
outputs/
*.pt
*.bin
*.safetensors
*.gguf

# Raw generated data (large, tracked in R2 not git)
data/*.jsonl
data/test_samples.jsonl
data/swarmcre_dataset/output/
data/swarmcre_dataset/.progress_*
data/router_v3/output/
data/audit_output/

# Sealed build DATA files (JSONL too large — on R2 + SHA256 verifiable)
# Manifests + SHA256SUMS ARE committed (provenance records)
data/router_v3/builds/**/*.jsonl

# Promoted + stale raw data dumps
aviation_promoted/
data/keeper/

# Worker
worker/node_modules/
worker/cf-api-token
.wrangler/

# Logs
*.log
trainer_log.jsonl
training.log

# Unsloth cache
unsloth_compiled_cache/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.key
*.env.*
